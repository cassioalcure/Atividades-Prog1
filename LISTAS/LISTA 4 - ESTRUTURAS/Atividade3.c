/*Crie um trecho de código em linguagem C que defina uma estrutura (struct) chamada tVetorR3 que
armazene:
• x (real),
• y (real) e
• z (real).*/

#include <stdio.h>

struct tVetorR3
{
    float x;
    float y;
    float z;
};
