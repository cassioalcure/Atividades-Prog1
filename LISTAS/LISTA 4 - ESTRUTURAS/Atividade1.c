/*Crie um trecho de código em linguagem C que defina uma estrutura (struct) chamada tRelogio que
armazene:
• hora (inteiro),
• minuto (inteiro) e
• segundo (inteiro).*/

#include <stdio.h>

struct tRelogio
{
    int hora;
    int minuto;
    int segundo;
};

